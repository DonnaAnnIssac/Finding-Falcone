import * as React from "react";
import {Route, Router} from "react-router";
import App from "../app";
export default (
    <Router>
        <Route path="/" component={App}>
        </Route>
    </Router>
)